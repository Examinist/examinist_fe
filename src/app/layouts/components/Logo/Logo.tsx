import { Typography } from '@mui/material';

export default function logo() {
  return (
    <Typography
      sx={{
        color: "#1B84BF",
        fontSize: "1.7rem",
        mx: 4,
        mt: 1,
        fontWeight: "bold",
      }}
    >
      Examinist
    </Typography>
  );
}
