import { Typography } from '@mui/material';

export default function logo() {
  return (
    <Typography
      sx={{
        color: "#1B84BF",
        fontSize: "1.9rem",
        fontWeight: "bold",
      }}
    >
      Examinist
    </Typography>
  );
}
