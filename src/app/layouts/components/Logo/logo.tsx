import { Typography } from '@mui/material';
import React from 'react'

export default function logo() {
  return (
    <Typography
      sx={{
        color: "#1B84BF",
        fontSize: "2rem",
        fontWeight: "bold",
      }}
    >
      Examinist
    </Typography>
  );
}
